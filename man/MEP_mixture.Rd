% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MEP_Mixture.R
\name{MEP_mixture}
\alias{MEP_mixture}
\title{Severity-Adaptive MEP for Mixture (multi-predictor) Logistic}
\usage{
MEP_mixture(
  y,
  X,
  burn_in = 1000,
  n_iter = 9000,
  init_beta = 0.01,
  step_size = 0.4,
  mu_intercept_offsets = seq(-1, 1, by = 0.2),
  sigma0_intercept = 10,
  sigma_global_multipliers = c(0.1, 0.5, 1, 2, 5, 10),
  sigma_hi = 5,
  sigma_lo = 0.15,
  kappa_min = 1,
  kappa_max = 2.5,
  kappa_delta = seq(-0.5, 0.5, by = 0.2),
  accept_window = c(0.3, 0.4),
  accept_target = 0.35,
  ref = NULL,
  transform_back = c("none", "logit", "SAS", "Long"),
  ci_level = 0.95,
  ppc_threshold = 0.8,
  return_draws = FALSE,
  n_chains = 1,
  chain_seeds = NULL,
  combine_chains = c("stack", "none"),
  tune_threshold_hi = 0.45,
  tune_threshold_lo = 0.2,
  tune_interval = 1000,
  ess_threshold = 150,
  geweke_z_threshold = 2
)
}
\arguments{
\item{y}{Numeric binary vector (0/1; logical or 2-level factor/character accepted; coerced to 0/1).}

\item{X}{Matrix or data.frame of predictors (no intercept). May include factors. Rows must align with y.}

\item{burn_in}{Integer; number of burn-in iterations per chain (discarded). Default 1000.}

\item{n_iter}{Integer; number of post-burn-in MCMC iterations (posterior draws) per chain. Default 9000.}

\item{init_beta}{Initial value(s) for the MH chain. Scalar (recycled) or numeric vector of length (1 + p_enc).
Default 0.01.}

\item{step_size}{Proposal standard deviation for RW-MH. Default 0.40.}

\item{mu_intercept_offsets}{Numeric vector of offsets added to logit(mean(y)) for intercept prior mean grid.
Default seq(-1, 1, by = 0.2).}

\item{sigma0_intercept}{Prior sd for the intercept (logit scale). Default 10.}

\item{sigma_global_multipliers}{Numeric vector of global multipliers applied to all slope prior scales.
Default c(0.1, 0.5, 1, 2, 5, 10).}

\item{sigma_hi}{Slope prior sd under mild separation (s=0). Default 5.}

\item{sigma_lo}{Slope prior sd under severe separation (s=1). Default 0.15.}

\item{kappa_min, kappa_max}{EP shape at s=0 and s=1. Defaults 1 and 2.5.}

\item{kappa_delta}{Offsets around anchor-average kappa to form the grid.
Default seq(-0.5, 0.5, by = 0.2), truncated to \code{[0.5, 3]}.}

\item{accept_window}{Numeric length-2 vector; acceptable MH acceptance interval. Default c(0.30, 0.40).}

\item{accept_target}{Scalar acceptance target used if no grid point is inside accept_window. Default 0.35.}

\item{ref}{Predictor name or index (in original X) to serve as ratio denominator. If NULL, uses max severity.}

\item{transform_back}{One of "none","logit","SAS","Long". Default "none".}

\item{ci_level}{Credible interval level in (0,1). Default 0.95.}

\item{ppc_threshold}{Posterior predictive match threshold. Default \code{0.80}.}

\item{return_draws}{Logical; if TRUE return post-burn draws for selected run.}

\item{n_chains}{Integer; number of MH chains to rerun for the selected best grid point. Default 1.}

\item{chain_seeds}{Optional integer vector of length n_chains for best-point reruns. If NULL, random seeds are generated.}

\item{combine_chains}{One of "stack" or "none". Default "stack".}

\item{tune_threshold_hi, tune_threshold_lo}{Burn-in acceptance thresholds for multiplicative tuning
(increase if > hi; decrease if < lo). Defaults 0.45 and 0.20.}

\item{tune_interval}{Iterations between tuning checks during burn-in. Default 1000.}

\item{ess_threshold}{Minimum ESS required for single-chain diagnostics (requires coda). Default 150.}

\item{geweke_z_threshold}{Maximum abs Geweke z allowed for single-chain diagnostics (requires coda). Default 2.}
}
\value{
A list with:
\item \code{best_settings}: list with chosen \code{mu} (string), \code{Sigma_diag} (string), chosen \code{kappa},
, \code{kappa_mode}, \code{acceptance_rate}, and \code{prop_matched}.
\itemize{
\item posterior_means, scaled_summary, standardized_coefs_back
\item diagnostics_single (if n_chains==1) or diagnostics_multiple (if n_chains>=2) when coda is available
\item burnin_step_trace_best, step_size_final_best
\item draws (optional)
\item plus: ref_predictor, severity, grid_summary
}
}
\description{
Fits a multi-predictor logistic model with a Multivariate Exponential Power (MEP)
prior using a Random-Walk Metropolis-Hastings (RW-MH) sampler. Slope prior scales
are anchored by univariate DISCO severities computed on the modeling data.
A small grid over intercept prior mean offsets, a global multiplier on slope scales,
and the EP shape kappa is explored. One run is selected using an acceptance-rate window,
posterior predictive agreement, and (when available) closeness to GLM coefficient ratios
relative to a reference predictor.
}
\details{
Complete-case only. This function assumes inputs are complete; it stops if any NA
appears in y or X.

Design encoding. Predictors in X are expanded with model.matrix(~ ., data = X)
(intercept dropped for slopes). Numeric columns remain one column each. Factor columns
are expanded to treatment-contrast dummies (baseline is the first level). RW-MH is run
on the standardized encoded design (z-scored columns). Back-transforms are reported
to the original (unscaled) encoded columns as b_A_original, b_SAS_original, b_Long_original.
}
\examples{
\donttest{
y <- c(0,0,0,0, 1,1,1,1)
X <- data.frame(
  X1 = c(-1.86,-0.81, 1.32,-0.40, 0.91, 2.49, 0.34, 0.25),
  X2 = c( 0.52,-0.07, 0.60, 0.67,-1.39, 0.16,-1.40,-0.09),
  X3 = factor(c(rep("A",4), rep("B",4)))
)

## Single chain
fit_single <- MEP_mixture(
  y, X,
  n_chains = 1,
  chain_seeds = 9
)
fit_single$scaled_summary
fit_single$diagnostics_single

## Multiple chains
fit_multi <- MEP_mixture(
  y, X,
  n_chains = 4,
  chain_seeds = c(101, 102, 103, 104),
  combine_chains = "stack"
)
fit_multi$scaled_summary
fit_multi$diagnostics_multiple
}
}
