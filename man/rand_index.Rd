% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rand_index.R
\name{rand_index}
\alias{rand_index}
\title{Vectorized Rand Index (symmetric, NA-safe)}
\usage{
rand_index(y_true, y_pred)
}
\arguments{
\item{y_true}{A vector of true cluster labels (integer, logical, or factor).}

\item{y_pred}{A vector of predicted cluster labels (integer, logical, or factor).}
}
\value{
A list with:
\itemize{
\item \code{rand_index} Rand index in \code{[0,1]} or \code{NA} if \eqn{n < 2}.
\item \code{a}, \code{b}, \code{c}, \code{d} Pair counts.
}
}
\description{
Computes the Rand index and pair counts (a, b, c, d) using a contingency
table, skipping any pairs with missing labels.
}
\examples{
ri <- rand_index(c(0,0,1,1), c(0,1,1,1))
ri$rand_index
}
